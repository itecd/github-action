name: hello-world
on: : push
jobs:
  hello-world-jobs:
    run-on: ubuntu-latest
    steps:
      - name: checkout repo code
        uses: action/checkout@v4
      - run: echo "$(cat hello_world.txt)"
      
